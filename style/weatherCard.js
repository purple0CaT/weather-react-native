import { StyleSheet } from "react-native";
//
const styles = StyleSheet.create({
  cardWrapper: { flex: 0.7 },
  card: {
    flex: 0.5,
    flexDirection: "row",
    borderRadius: 10,
    backgroundColor: "white",
    margin: 10,
    padding: 5,
  },
});

export default styles;
